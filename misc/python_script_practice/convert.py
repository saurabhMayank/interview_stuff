import json
'''
In initial formatting we r removing all the @ from the keys and values..In this we will use regex to solve the problem
'''
def initial_formatting(data):
    data_new =


  





with open('temp.json', 'r+') as read_file:
    data = json.loads(read_file.read())
    data_1=initial_formatting(data)